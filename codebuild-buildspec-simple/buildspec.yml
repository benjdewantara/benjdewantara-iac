version: 0.2
env:
  secrets-manager:
    CREDENTIALS_GIT_REPO: "${secretsmanager_name}"
phases:
  build:
    commands:
      - echo "Hello World"
      - echo "Secrets value of credentials-git-repo is $CREDENTIALS_GIT_REPO"